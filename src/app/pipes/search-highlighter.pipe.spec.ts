import { SearchHighlighterPipe } from './search-highlighter.pipe';

describe('SearchHighlighterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchHighlighterPipe();
    expect(pipe).toBeTruthy();
  });
});
